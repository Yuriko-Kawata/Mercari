// test-data.js

export const testUserData = {
    email: 'test1@example.com',
    password: 'Test1234'
  };

// テストケース内で以下のように利用する
// import { testUserData } from './test-data.js';
// await login(page, testUserData.email, testUserData.password);